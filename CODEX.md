# Codex Instructions

Follow the canonical project instructions in `agents/shared.md`.

Codex-specific notes:

- Read nearby files before editing and match local naming, layout, and CSS Module style.
- Use the atomic design folders as ownership boundaries.
- Keep client components narrow; default to server components in `src/app` and templates.
- Before completing code changes, run `npm run typecheck`, `npm run lint`, and `npm run build`.
